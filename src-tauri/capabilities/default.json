{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for the INTP widget",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}

